# Cloudflare Pages redirects — handle default-locale routing
# (next-intl middleware doesn't run with static export, so we route here.)

# Bare paths → default locale (Dutch)
/services      /nl/services      301
/work          /nl/work          301
/about         /nl/about         301
/blog          /nl/blog          301
/contact       /nl/contact       301
